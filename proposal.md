期末專案企劃書：即時公車導航與步行時間最佳化地圖系統
一、 專案動機與解決問題 現有的地圖導航服務在規劃大眾運輸時，公車的預估抵達時間往往依賴靜態班表，導致使用者可能面臨提早到達卻錯過公車，或是白白在站牌等待的窘境。本專案旨在建立一個以「即時公車動態」為核心的路線規劃平台，精確拆解並計算「步行至站牌」、「等待時間」、「乘車時間」與「下車後步行時間」，提供使用者最符合當下真實情況的交通方案。
二、 核心功能描述
地點解析與定位：允許使用者輸入目的地關鍵字，系統將自動解析為精確的地理座標。
多維度時間計算：輸入目的地後，系統會結合政府即時公車 API 與步行計算服務，運算涵蓋起迄點步行與乘車的完整時間。
最佳路線排序：依據總花費時間（步行＋等待＋乘車），列出前 10 筆最快抵達的搭乘方案，並明確標示各階段的耗時細節。
互動式路線地圖：將規劃出的步行路徑、公車站牌位置與預估路線繪製於網頁地圖上，提供直覺的視覺引導。
三、 系統架構與技術實作 專案將以 Flask 作為網頁伺服器後端，依循模組化設計原則，避免將所有程式碼集中於單一檔案 。核心 API 串接邏輯如下：

地理編碼模組 (Nominatim API)：負責接收使用者在前端輸入的目標地點，將其轉換為經緯度座標 。

即時公車資料模組 (TDX 運輸資料流通服務)：串接政府開放的 API，獲取使用者起點與終點周邊半徑內的公車站牌位置，以及行經該站牌的即時公車預估到站時間與行駛數據。
步行時間過濾模組 (OSRM API)：將起點座標至上車點、下車點至終點座標送入 OSRM，取得考量實際街道的真實步行時間與路徑 。

資料整合與排序演算法：後端商業邏輯將上述數據進行合併，篩選並計算出總耗時最短的方案組合 。考量到 API 呼叫的延遲，將引入快取機制與速率限制來優化效能 。
地圖視覺化模組 (Folium)：將最終排序完成的路線資訊、站牌標記與 OSRM 步行路徑交由 Folium 渲染為互動式地圖，並嵌入至前端 HTML 頁面中展示 。

像各個站牌位置、編號、各個公車的路線應該要先儲存在本機的檔案裡面（希望可以用json儲存）


【Tech Stack Requirements】
你必須使用以下技術棧進行開發，缺乏任何一項核心 API 將導致專題零分：

後端框架：Python Flask   
地理編碼 API：必須使用 Nominatim API 來搜尋地點與候選位置 。  

路徑規劃 API：必須使用 OSRM API (Open Source Routing Machine) 來取得真實的步行或行車時間，並以此進行資料過濾與排序 。  

地圖渲染：必須將結果呈現於互動式的 Leaflet Map 上，且此地圖必須透過 Python 的 Folium 套件生成，並嵌入於網頁中 。  

【Project Architecture Constraints】
為了符合「良好的專案管理」評分標準，絕對不可以將所有程式碼寫在同一個檔案中 。： 

幫我生成的時候勁量把功能切分清楚，因為我需要閱讀完所有代碼。
